name: mint
description: Mint.

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  cloud_firestore: ^2.5.3
  firebase_auth: ^3.1.3
  firebase_core : ^1.7.0
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  provider: ^6.0.1

dev_dependencies:
  build_runner: ^2.1.4
  flutter_test:
    sdk: flutter
  import_sorter: ^4.6.0
  pedantic: ^1.11.1

flutter:
  uses-material-design: true

import_sorter:
  comments: false